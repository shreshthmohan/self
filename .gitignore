.scratch/
.DS_Store
node_modules/

# Secrets — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Cloudflare and build output
.wrangler/
build/
dist/
.react-router/
*.tsbuildinfo

# Playwright: the dev server log, the owner's cookies, reports, traces
tests/.tmp/
